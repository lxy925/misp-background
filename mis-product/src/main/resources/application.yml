server:
  port: 8083

spring:
  application:
    name: mis-product
  profiles:
    active: dev
  cloud:
    nacos:
      server-addr: 127.0.0.1:8848

mybatis:
  mapper-locations: classpath:mapper/*.xml
  configuration:
    map-underscore-to-camel-case: true